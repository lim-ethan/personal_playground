```yaml
version: '3.8'
services:
  zookeeper:
    image: confluentinc/cp-zookeeper:7.4.0
    container_name: zookeeper
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    ports:
      - "2181:2181"
    networks:
      - elk-net
  kafka:
    image: confluentinc/cp-kafka:7.4.0
    container_name: kafka
    depends_on:
      - zookeeper
    ports:
      - "9092:9092"
      - "29092:29092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092,PLAINTEXT_HOST://localhost:29092
      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:9092,PLAINTEXT_HOST://0.0.0.0:29092
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
    networks:
      - elk-net
  kafka-ui:
    image: provectuslabs/kafka-ui:latest
    container_name: kafka-ui
    ports:
      - 8080:8080
    environment:
      - KAFKA_CLUSTERS_0_NAME=local-cluster
      - KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS=kafka:9092
    networks:
      - elk-net
  mysql:
    image: mysql:5.7
    container_name: mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: metastore
      MYSQL_USER: hive
      MYSQL_PASSWORD: hive
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql
    networks:
      - elk-net
  hive-metastore:
    image: apache/hive:3.1.3
    container_name: hive-metastore
    depends_on:
      - mysql
    environment:
      SERVICE_NAME: metastore
      HIVE_METASTORE_DB_TYPE: mysql
    ports:
      - "9083:9083"
    networks:
      - elk-net
  spark:
    build: ./spark-dockerfile
    container_name: spark-jupyter
    ports:
      - "8888:8888"
    environment:
      - JUPYTER_ENABLE_LAB=yes
    volumes:
      - ./notebooks:/home/jovyan/work
      - ./spark/conf/hive-site.xml:/usr/local/spark/conf/hive-site.xml  # Hive 메타스토어 연동
    networks:
      - elk-net
  mongo:
    image: mongo:5.0
    container_name: mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    networks:
      - elk-net
  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:7.17.0
    container_name: elasticsearch
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
      - ES_JAVA_OPTS=-Xms1g -Xmx1g
    ports:
      - "9200:9200"
      - "9300:9300"
    volumes:
      - elasticsearch-data:/usr/share/elasticsearch/data
    networks:
      - elk-net
  logstash:
    image: docker.elastic.co/logstash/logstash:7.17.0
    container_name: logstash
    ports:
      - "5050:5044"
    depends_on:
      - elasticsearch
    environment:
      - "LOGSTASH_JAVA_OPTS=-Xmx512m -Xms256m"
    volumes:
      - ./logstash/pipeline:/usr/share/logstash/pipeline
    networks:
      - elk-net
  kibana:
    image: docker.elastic.co/kibana/kibana:7.17.0
    container_name: kibana
    ports:
      - "5601:5601"
    depends_on:
      - elasticsearch
    environment:
      - ELASTICSEARCH_HOSTS=http://elasticsearch:9200
    networks:
      - elk-net
  grafana:
    image: grafana/grafana:9.1.6
    container_name: grafana
    ports:
      - "3000:3000"
    volumes:
      - grafana-data:/var/lib/grafana
    networks:
      - elk-net
  openmetadata:
    image: openmetadata/server:latest
    container_name: openmetadata
    ports:
      - "8585:8585"
    environment:
      SERVER_HOST: openmetadata
      OM_DATABASE_URL: jdbc:mysql://mysql:3306/metastore
      OM_DATABASE_HOST: mysql
      OM_DATABASE_PORT: 3306
      OM_DATABASE_NAME: metastore
      OM_DATABASE_USER: hive
      OM_DATABASE_PASSWORD: hive
      AUTH_PROVIDER: no-auth
      ELASTICSEARCH_HOST: http://elasticsearch:9200
    depends_on:
      - mysql
      - elasticsearch
    networks:
      - elk-net
  openmetadata-ingestion:
    image: openmetadata/ingestion:latest
    container_name: openmetadata-ingestion
    command: airflow standalone
    ports:
      - "8081:8080"  # Airflow UI 접근
    environment:
      - AIRFLOW_HOME=/opt/airflow
    volumes:
      - ./airflow-dags:/opt/airflow/dags
    networks:
      - elk-net
volumes:
  mongo-data:
  grafana-data:
  mysql-data:
  elasticsearch-data:
networks:
  elk-net:
    driver: bridge


```